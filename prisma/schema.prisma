generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Ticket {
  id          String   @id @default(cuid())
  orderId     String   @unique

  // identity
  fullName          String
  nickname          String
  email             String
  phone             String
  domisili          String            @default("")
  participantStatus String            // Student / Fresh Graduate / Professional
  studentId         String?
  faculty           String?
  institution       String?
  major             String?
  instagram         String?
  linkedin          String?

  // persona (long-answer -> quick choice)
  tedFamiliarity    String
  topics            String[] @default([])
  topicsOther       String?
  musicLifestyle    String   @default("")
  environmentShapes String   @default("")
  artsExpression    String   @default("")
  eventTakeaway     String   @default("")
  eventAspect       String[] @default([])
  eventAspectOther  String?

  // consent
  consentAccurate       Boolean @default(false)
  consentDataProcessing Boolean @default(false)
  consentUpdates        Boolean @default(false)

  // ticket & payment
  tier        String
  price       String
  paymentName String?
  proofUrl    String?
  status      TicketStatus @default(PENDING)
  qrToken     String       @unique @default(cuid())
  scanned     Boolean      @default(false)
  scannedAt   DateTime?
  createdAt   DateTime     @default(now())

  qrCodeBase64 String? @db.Text
}

model Speaker {
  id          String   @id @default(cuid())
  name        String
  title       String?
  description String?

  mainPhoto        String
  supportingPhoto1 String?
  supportingPhoto2 String?
  supportingPhoto3 String?

  isLocked    Boolean    @default(true)
  revealedAt  DateTime?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}

enum TicketStatus {
  PENDING
  CONFIRMED
  REJECTED
}
